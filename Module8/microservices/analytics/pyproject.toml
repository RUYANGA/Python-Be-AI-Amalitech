[project]
name = "analytics-service"
version = "1.0.0"
description = "Analytics microservice: click ingestion and premium detailed analytics"
requires-python = ">=3.12"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
pythonpath = ["src"]
testpaths = ["src/apps"]
python_files = ["test_*.py"]
