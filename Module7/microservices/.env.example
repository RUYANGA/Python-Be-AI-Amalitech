DEBUG=True

AUTH_SECRET_KEY=change-me-auth
SHORTENER_SECRET_KEY=change-me-shortener
ANALYTICS_SECRET_KEY=change-me-analytics

# Shared static secret for internal service-to-service REST calls (token
# validation, ownership lookup, click ingestion). Not a JWT, not
# user-facing — just needs to match on both sides.
INTERNAL_SERVICE_TOKEN=change-me-internal-token

# All three services connect to Postgres running on the HOST machine
# (via host.docker.internal), not a containerized database — one role,
# three separate databases. Create them once with e.g.:
#   createdb -U postgres auth shortener analytics
DB_USER=postgres
DB_PASSWORD=change-me

AUTH_DB_NAME=auth
SHORTENER_DB_NAME=shortener
ANALYTICS_DB_NAME=analytics
