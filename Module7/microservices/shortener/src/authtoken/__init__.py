"""Generated gRPC/protobuf contract for the internal token-validation lookup.

This package is generated from ``authtoken.proto`` via ``grpcio-tools``:

    python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. \\
        authtoken/authtoken.proto

It defines the ``authtoken.AuthTokenValidation`` service shared between the
auth (server) service and its callers (shortener, analytics — the
clients). Do not edit the ``*_pb2*.py`` files by hand — regenerate from the
``.proto`` instead.
"""
